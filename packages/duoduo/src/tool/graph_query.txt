Query the knowledge graph for BROAD STRUCTURAL code intelligence across the whole project: large-scale dependencies, similar functions, and call chains between distant modules. Prefer this over grep for discovering relationships at scale. Use the LSP tool (lsp) instead when you need PRECISE, TYPE-AWARE results for a specific symbol at a known file/line/character (exact go-to-definition, find references, hover type info, call hierarchy). Returns nodes and edges from the project's code graph.
